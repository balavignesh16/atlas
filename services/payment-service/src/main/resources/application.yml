server:
  port: ${PAYMENT_SERVICE_PORT:8086}
  shutdown: graceful

spring:
  application:
    name: payment-service

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
