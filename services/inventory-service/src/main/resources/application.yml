server:
  port: ${INVENTORY_SERVICE_PORT:8085}

spring:
  application:
    name: inventory-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
