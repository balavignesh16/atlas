# Base URL of the Atlas intelligence-engine HTTP API.
# The API key itself is entered at runtime via the login screen and kept
# only in sessionStorage -- it is never configured here or committed.
VITE_ATLAS_API_URL=http://localhost:8081
